# Copy this file to .env and fill values as needed.
# The app can run without this key using heuristics, but AI parsing works better with it.
OPENAI_API_KEY=your_openai_api_key_here

# Optional override
# OPENAI_MODEL=gpt-4o-mini
